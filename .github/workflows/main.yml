name: Build project

on:
   workflow_dispatch:
permissions:
  contents: write

jobs:
  buildForAllSupportedPlatforms:
    name: Build for Android
    runs-on: ubuntu-latest
#     strategy:
#       fail-fast: false
#       matrix:
#         targetPlatform:
# #          - StandaloneWindows64 # Build a Windows 64-bit standalone.
# #          - WebGL # WebGL.
#           - Android # Build an Android .apk standalone app.
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
          lfs: true
      # - uses: actions/cache@v2
      #   with:
      #     path: Library
      #     key: Library-${{ matrix.targetPlatform }}
      #     restore-keys: Library-
      - uses: game-ci/unity-builder@v2
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
        with:
          unityVersion: 2020.3.47f1
          targetPlatform: Android
          buildMethod: ContinuousIntegration.BuildWin # Addressable content build is triggered by the build method
          versioning: Semantic
      #   customParameters: -devBuild ${{ github.event.inputs.build_type }} -branch ${{ steps.extract_branch.outputs.branch }}
      #   with:
      #     targetPlatform: ${{ matrix.targetPlatform }}
      - uses: actions/upload-artifact@v2
        with:
          name: Build-Android
          path: build/Android/Android.apk
  # deployPages:
  #   needs: buildForAllSupportedPlatforms
  #   name: Deploy to Github Pages 🚀
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v2

  #     - uses: actions/download-artifact@v2
  #       with:
  #         name: Build-WebGL 
  #         path: build/WebGL 

  #     - name: Deploy 🚀
  #       uses: JamesIves/github-pages-deploy-action@4.1.4
  #       with:
  #         branch: gh-pages
  #         folder: build/WebGL
